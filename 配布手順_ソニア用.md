# 配布手順（ソニアが1回だけやる）

ねこ・ぬこが「ダブルクリックだけ」で使えるようにするための、ソニア側の初回セットアップ。
**キー類はソニアのぶんを同梱するので、他の2人はトークン設定を一切やりません。**

## 前提（3人全員に必要なもの）
- **Chrome/Edge** … 音声認識(Web Speech)標準搭載・追加インストール不要
- **Node.js**（LTS） … Claude Code が動いていれば入っているはず（確認: `node --version`）
- **Claude Code（claudeコマンド）にログイン済み** … 各自のClaude Maxで。
  - ※LLMは各自のPCのサブスクで動くので、**ここだけは3人それぞれ**が必要
  - ⚠「ブラウザでclaude.aiにログイン」ではなく、**ターミナルで `claude auth login`** を1回。確認は `claude auth status`（`"loggedIn": true`）
  - 未ログインだとアプリ画面上部に黄色いバナーで案内が出る（他2人にはこれで十分伝わる）
  - 他に必要なアカウントは無し（Miroはソニアのを同梱、音声は無料Web Speech）

## ソニアがやること

### 1. 自分のキーを環境変数に入れる（未済なら）
```
setx MIRO_TOKEN "（Miroトークン）"
setx SONIOX_API_KEY "（Sonioxキー）"
```
（取得方法は `docs/SETUP_MIRO.md` / `docs/SETUP_STT.md`）

### 2. 同梱設定ファイルを生成
新しいターミナルで、このフォルダで:
```
node tools/make-config.js
```
→ `config.local.json` が作られる（**Miroトークンのみ**焼き込み）。
- これで受け取ったねこ・ぬこは**無料のWeb Speech**（Chrome標準搭載・インストール不要）で動きます。
- あなた自身は環境変数のSonioxがそのまま優先されるので高品質のまま。
- ※もし二人にもSonioxを使わせたい（＝あなたのクレジット消費）場合のみ `node tools/make-config.js --with-stt`。
- 音声認識を強化したいと言われたら「Sonioxの課金（各自 or 共有）が必要」と伝えればOK。

### 3. Miroを有料プランにしてリンク共有を有効化（※重要）
- Miroを有料プランにアップグレードすると、生成されるボードが**「リンクを知っていれば誰でも編集可」**になり、ねこ・ぬこは**ログイン無しでボードURLを開くだけ**になります。
- 無料のままだと、ボードはソニアのアカウント内に作られ、他2人は閲覧にソニアのMiroログインが要ります（アップグレード後はコード変更不要で自動的にリンク共有化）。

### 4. フォルダごと2人に渡す
この `Voice-Miro` フォルダを**まるごと**（`config.local.json` を含めて）zip等でねこ・ぬこに送る。

## ねこ・ぬこがやること（これだけ）
1. もらったフォルダを好きな場所に置く
2. **「Voice-Miro を起動」をダブルクリック**（Windows=.bat / Mac=.command）
3. ブラウザが自動で開く → テンプレを選んで「セッション開始」→ 喋る

※Macの初回のみ「開発元が未確認」と出たら、ファイルを右クリック→「開く」で1回許可すればOK。

## 使い方（3人共通）
- ダブルクリック → ブラウザが `http://localhost:7788` を自動で開く
- 停止するには、起動時の黒いウィンドウ（ターミナル）を閉じる
- 2回目以降ダブルクリックしても、既に起動中ならブラウザを開くだけ

## 更新を配るとき
このフォルダを直したら、また zip で送るだけ（config.local.json は各自のを維持したいので、渡す前に相手ぶんに差し替えるか、共通キーで運用）。
